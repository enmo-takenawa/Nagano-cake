
  <table class="table table-bordered">
    <thead>
      <tr>ジャンル検索</tr>
    </thead>

    <tbody>
      <% genres.each do |genre| %>
      <tr>
        <td><%= link_to genre.name %></td>
      </tr>
      <% end %>
    </tbody>
  </table>

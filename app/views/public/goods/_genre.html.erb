
  <table class="table table-bordered">
    <thead>
      <tr>
        <th class="text-dark">ジャンル検索</th>
      </tr>
    </thead>

    <tbody class="table-borderless">
      <% genres.each do |genre| %>
      <tr>
        <td><%= link_to genre.name,search_path('search[value]': genre.id, 'search[how]': "match") %></td>
      </tr>
      <% end %>
    </tbody>
  </table>

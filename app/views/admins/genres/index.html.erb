<div class="container">
    <div class="row">
        <h4 class="red-border bg-light m-4">ジャンル一覧</h4>
    </div>
    <div class="row mt-4">
        <div class='col'>
            <%= form_with model: @genre, url: admins_genres_path, local:true do |f| %>
            <table>
                <tbody>
                    <tr>
                        <td class="text-dark" width="150">ジャンル名</td>
                        <td width="200"><%= f.text_field :name, placeholder: "ジャンル名" %></td>
                        <td><%= f.submit "新規登録", class:"btn btn-success" %></td>
                    </tr>
                </tbody>
            </table>
            <% end %>
        </div>
    </div>

    <div class="row mt-5">
        <div class="col-md-4">
            <table class="table table-hover table-inversey">
                <thead class="bg-light">
                    <tr>
                        <th class="text-dark">ジャンル</th>
                        <td></td>
                    </tr>
                </thead>
                <tbody>
                    <% @genres.each do |genre| %>
                        <tr>
                            <td><%= genre.name %></td>
                            <td><%= link_to "編集する", edit_admins_genre_path(genre.id), class: "btn btn-sm btn-success" %></td>
                        </tr>
                    <% end %>
                </tbody>
            </table>
        </div>
    </div>

</div>